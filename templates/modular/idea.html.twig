<section id="idea">
	<div class="maxwidth">
		<div class="idea-wrapper">
			<h1>{{ 'LANDING.SLOGAN'|t }}</h1>
			<div id="idea-text">
			
			{{ page.content }}
			
			</div>
			<button id="idea-button" type="button" data-text-show="{{ 'LEARN_MORE'|t }}" data-text-hide="{{ 'HIDE'|t }}">{{ 'LEARN_MORE'|t }}</button>
		</div>
	</div>
</section>